DB_USER=postgres
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=pixelgrid

# MongoDB (optional - used for logs/analytics)
MONGO_URI=mongodb://localhost:27017/pixelgrid

# JWT secrets
JWT_ACCESS_SECRET=change_this_to_a_long_random_string
JWT_REFRESH_SECRET=change_this_to_another_long_random_string

# Frontend URL (CORS)
FRONTEND_URL=http://localhost:5173

# Server settings
PORT=3001
NODE_ENV=development

# Optional: skip DB initialization (useful for some CI or lightweight runs)
# Set to 'true' to skip DB connection checks
SKIP_DB=false
